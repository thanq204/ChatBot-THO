# Embedding câu trả lời FAQ

Chỉ embedding câu trả lời FAQ đã duyệt khi hệ thống cần retrieval theo answer. Liên kết mỗi vector với `faq_id` và version của answer để answer sửa đổi tạo embedding mới.
