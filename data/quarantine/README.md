# Quarantine

Dữ liệu thất bại, không hợp lệ hoặc đáng ngờ đưa vào đây thay vì xóa. Dùng `failed_parses/`, `failed_embeddings/` hoặc thư mục con có tên rõ ràng khác. Khi có thể, luôn ghi lý do lỗi và source ID.
