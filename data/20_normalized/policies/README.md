# Policy đã chuẩn hóa

Lưu policy moderation theo schema chuẩn với `policy_id`, name, description, category, action, trigger terms, active flag, version và timestamp.

Policy được tách khỏi knowledge vì policy điều khiển quyết định moderation, không chỉ cung cấp context cho RAG.
